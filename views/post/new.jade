!!! 5
html
	head
		title Add New Event
		script
			$('#h3centergoings').text("Add New Event");
	body
		b Add New Event
		form(action='/post', method='post', enctype='multipart/form-data')!= partial('post/post_form', {locals: {post: post}})
			p
				input(type='submit')